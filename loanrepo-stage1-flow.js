/* LoanRepo Stage-1 flow: diagnosis -> report -> personalised guide -> Stage 2.
   The existing Stage-2 bridge remains available, but its original Track button
   is hidden here so the paid tracking journey is offered only after the guide. */
(function () {
  var db = window.LoanRepoDB;
  if (!db) return;
  var PENDING = "loanrepo.pending_track", DIAG = "loanrepo.pending_diagnosis", CHECKOUT = "loanrepo.pending_checkout";
  var user = null, busy = false;
  function isPage(){var p=(location.pathname||"").toLowerCase();return p==="/"||/\/index\.html$/.test(p);}
  if(!isPage())return;
  function set(k,v){try{sessionStorage.setItem(k,v)}catch(e){}}
  function get(k){try{return sessionStorage.getItem(k)}catch(e){return null}}
  function del(k){try{sessionStorage.removeItem(k)}catch(e){}}
  function buttons(){return Array.prototype.slice.call(document.querySelectorAll("button"));}
  function oldTrack(){return buttons().find(function(b){return /^Track this loan/.test((b.textContent||"").trim())});}
  function book(){return buttons().find(function(b){return /THE BOOK/i.test(b.textContent||"")&&/₹?\s*299/.test(b.textContent||"")});}
  function snapshot(){var d=document.getElementById("lr-date"),t=document.getElementById("lr-ten"),a=document.getElementById("lr-amt");if(!d||!t||!a)return null;var bm=document.querySelector('input[name="bm"]:checked'),sp=document.getElementById("lr-spread");var dark=Array.prototype.slice.call(document.querySelectorAll("div")).find(function(x){return /Added to your loan, unannounced/.test(x.textContent||"")});return{date:d.value,tenure:Number(t.value),amount:Number(a.value),benchmark:bm?bm.value:"EBLR",spread:sp?sp.value:"",verdict_text:dark?(dark.textContent||"").replace(/\s+/g," ").slice(0,500):""};}
  function savePending(){var d=snapshot();if(!d)return false;set(DIAG,JSON.stringify(d));set(PENDING,"1");return true;}
  function active(s){return !!(s&&s.plan==="pro"&&s.status==="active");}
  function loadRazorpay(){if(window.Razorpay)return Promise.resolve(true);return new Promise(function(resolve){var s=document.createElement("script");s.src="https://checkout.razorpay.com/v1/checkout.js";s.onload=function(){resolve(!!window.Razorpay)};s.onerror=function(){resolve(false)};document.head.appendChild(s);});}
  function checkout(){if(get(CHECKOUT)==="opening")return;set(CHECKOUT,"opening");db.fetchSubscription().then(function(s){if(active(s)){del(CHECKOUT);del(PENDING);del(DIAG);location.href="app.html";return null}return loadRazorpay().then(function(ok){if(!ok)throw Error("checkout");return db.startCheckout()})}).then(function(r){if(!r)return;if(!r.ok||!r.subscriptionId)throw Error("checkout");var rz=new Razorpay({key:r.keyId,subscription_id:r.subscriptionId,name:"LoanRepo",description:"Loan Watch — ₹149/month",prefill:{email:user&&user.email||""},theme:{color:"#5980a6"},handler:function(){var n=0;(function poll(){db.fetchSubscription().then(function(s){if(active(s)){del(CHECKOUT);del(PENDING);del(DIAG);location.href="app.html"}else if(n++<20)setTimeout(poll,2000);else del(CHECKOUT)})})()},modal:{ondismiss:function(){del(CHECKOUT)}}});rz.open()}).catch(function(){del(CHECKOUT);busy=false;alert("Loan saved, but Loan Watch checkout could not be opened. Please try again.")});}
  function trackNow(){if(busy)return;var d=snapshot();if(!d)return;savePending();if(!user){var sign=buttons().find(function(b){return /^Sign in$/i.test((b.textContent||"").trim())});if(sign){sign.click();return}alert("Please sign in to continue to Loan Watch.");return}busy=true;var run={name:d.date+" · ₹"+Number(d.amount).toLocaleString("en-IN"),label:d.date+" · ₹"+Number(d.amount).toLocaleString("en-IN"),start_month:d.date,amount:d.amount,tenure_years:d.tenure,benchmark:d.benchmark,result:{verdict:d.verdict_text||"Saved diagnosis",diagnosis_snapshot:d}};db.saveRun(run).then(function(r){if(!r.ok){busy=false;alert("We couldn't save the loan just now. Nothing was lost — please try again.");return}db.track("loan_tracked",{stage:"post_stage1",benchmark:d.benchmark});del(PENDING);checkout()})}
  function findBookSection(){var bs=book(),all=Array.prototype.slice.call(document.querySelectorAll("div"));if(!bs)return null;return all.filter(function(x){var tx=x.textContent||"";return /THE PDF ABOVE/i.test(tx)&&/THE BOOK/i.test(tx)&&/eight chapters/i.test(tx)}).sort(function(a,b){return a.textContent.length-b.textContent.length})[0]||bs.parentElement;}
  function render(){var b=book(),tr=oldTrack(),section=findBookSection();if(tr){tr.style.display="none";tr.setAttribute("data-stage2-hidden","true");}if(b){var txt=b.textContent||"";if(/THE BOOK/i.test(txt))b.textContent="GET YOUR PERSONALISED BORROWER'S GUIDE — ₹299";}if(!section)return false;var existing=document.getElementById("lr-stage2-after-book");if(existing)return true;var wrap=document.createElement("div");wrap.id="lr-stage2-after-book";wrap.style.cssText="margin-top:20px;padding:18px 0 0;border-top:1px dashed var(--color-divider);display:flex;align-items:center;gap:16px;flex-wrap:wrap";var note=document.createElement("div");note.style.cssText="font-size:12px;line-height:1.5;color:var(--color-neutral-700);margin-right:auto";note.innerHTML="<b>Stage 2</b> · Once you've used your personalised guide, keep LoanRepo watching the loan for ₹149/month.";var btn=document.createElement("button");btn.type="button";btn.className="btn btn-primary";btn.textContent="TRACK THIS LOAN — ₹149/MONTH";btn.addEventListener("click",trackNow);wrap.appendChild(note);wrap.appendChild(btn);section.parentNode.insertBefore(wrap,section.nextSibling);return true;}
  db.onAuth(function(u){user=u||null});
  var mo=new MutationObserver(function(){render()});mo.observe(document.documentElement,{childList:true,subtree:true});setTimeout(function(){mo.disconnect()},60000);render();
})();
